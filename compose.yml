services:

  frontend:
    image: ${APP_IMAGE:-ghcr.io/alexindevs/anpmplagos-conference:latest}
    container_name: anpmp-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env.local
